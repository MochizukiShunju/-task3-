<html lang="ja">
  <head>
    <mate charset="UTF-8" />
    <link rel="stylesheet" href="application.css" />
  </head>

  <heder>

  </heder>

  <body>
    <main class="format">
      <%= render 'layouts/body' %>
      <div>
        <h1>Books</h1>
        <table>

          <tr>
            <th
            <th>image</th>
            <th>name</th>
          </tr>

          <% @books.each do |book| %>
            <tr>
              <td><%= attachment_image_tag @user, :profile_image, format: 'jpeg', fallback: "no_image.jpg", size: "60x60" %><br></td>
              <td><%= book.title %></td>
              <td><%= book.body %></td>
            </tr>
          <% end %>

        </table>
      </div>

    </main>

  </body>

  <footer>
   <%= render 'layouts/footer' %>
  </footer>

</html>
